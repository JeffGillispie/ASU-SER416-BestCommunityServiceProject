<!-- views/pages/request -->

<!DOCTYPE html>
<html lang="en">

  <head>
    <% include ../partials/head %>
  </head>

  <body class="container">

    <header>
      <% include ../partials/header %>
    </header>

    <main>

      <h2>Request a Service</h2>

      <% if (failMessage.length > 0) { %>
        <div class="alert alert-danger"><%= failMessage %></div>
      <% } %>

      <% if (successMessage.length > 0) { %>
        <div class="alert alert-success"><%= successMessage %></div>
      <% } %>

      <p>Don't see a service you are looking for? Request it and we'll do our best to add it to our array of provided services.</p>

      <div class="row">
        <div class="col-sm-6">
          <form class="form-horizontal" action="/request" method="post">
            <div class="form-group">
              <label class="control-label col-sm-3">Service Name</label>
              <div class="col-sm-9">
                <input type="text" class="form-control" name="serviceName" placeholder="Service Name" />
              </div>
            </div>

            <div class="form-group">
              <label class="control-label col-sm-3">Service Type</label>
              <div class="col-sm-9">
                <select class="form-control" name="serviceType">
                  <option>Event</option>
                  <option>Class</option>
                  <option>External</option>
                  <option>Catering</option>
                  <option>Rental</option>
                </select>
              </div>
            </div>

            <label class="control-label">Service Description</label>
            <textarea class="form-control" name="serviceDescription" rows="4"></textarea>

            <br /><br />

            <button type="submit" class="btn btn-primary">Submit</button>

            <br /><br />

          </form>
        </div>
      </div>
    </main>

    <footer>
      <% include ../partials/footer %>
    </footer>

  </body>

</html>
